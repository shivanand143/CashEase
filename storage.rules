
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read/write access for authenticated users
    // Restrict further based on specific needs (e.g., user folders)
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
